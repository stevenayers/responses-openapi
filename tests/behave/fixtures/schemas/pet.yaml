type: object
required:
  - id
  - name
properties:
  id:
    type: integer
    format: int64
  name:
    type: string
    minLength: 1
    maxLength: 100
  tag:
    type: string
    enum: [dog, cat, bird, fish]
